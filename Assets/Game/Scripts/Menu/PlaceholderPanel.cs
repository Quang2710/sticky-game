namespace StickmanTrap.Menu
{
    /// <summary>
    /// A "COMING SOON" screen. Concrete subclasses exist so the navigation wiring
    /// and Inspector references are already in place — when a feature ships, its
    /// panel class gets real content and the placeholder text is removed.
    /// </summary>
    public abstract class PlaceholderPanel : MenuPanel { }
}
